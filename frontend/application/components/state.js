const appState = {
    currentStructure: null,
    currentNode: null,
    activeTab: 'structures',
    isProcessing: false,
    jobs: [],
    nodeTypes: ['start', 'process']
};

export default appState;